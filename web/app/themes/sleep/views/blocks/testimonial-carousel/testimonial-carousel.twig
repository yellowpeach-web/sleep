{% extends "components/block-wrapper.twig" %}
{% block block_content %}

{% if testimonials %}
<div class="swiper testimonial-swiper" aria-label="Testimonials Carousel" role="region">
      <div class="swiper-wrapper">
            {% for item in testimonials %}
            {% if color_profile == 'dark-theme' %}
            {% set logo = item.meta('white_company_logo') %}
            {% else %}
            {% set logo = item.meta('blue_company_logo') %}
            {% endif %}
            <div class="swiper-slide w-[327px] md:w-[608px] xl:w-[736px] rounded-lg bg-surface-primary-bg-fill-default h-fit"
                  itemscope itemtype="https://schema.org/Review">
                  <div itemprop="itemReviewed" itemscope itemtype="https://schema.org/Organization" hidden>
                        <meta itemprop="name" content="Sleep.ai">
                  </div>
                  <div
                        class="flex flex-col p-8 md:p-14 gap-6 text-text-heading-fill-primary items-center justify-center h-full p-sm sm: ">
                        <div class="max-w-[120px] max-h-[32px]">
                              {% if logo %}
                              {{ responsive_img(
                              logo, {
                              size: 'large',
                              class: 'object-contain w-auto h-full max-w-[120px] max-h-[32px]',
                              })
                              }}
                              {% endif %}
                        </div>
                        {% if item.quote %}
                        <p class="!m-0 title-large text-text-heading-fill-primary text-center" itemprop="reviewBody">
                              {{item.quote}}
                        </p>

                        {% endif %}
                        <div class="flex flex-col gap1.5 items-center" itemprop="author" itemscope
                              itemtype="https://schema.org/Person">
                              {% if item.title %}
                              <h3 class="m-0 subtitle-large text-center" itemprop="name">
                                    {{item.title}}
                              </h3>
                              {% endif %}
                              {% if item.role %}
                              <h4 class="m-0 text-text-heading-fill-highlight-3 subtitle-large text-center">
                                    <span itemprop="jobTitle">{{ item.role }}</span>
                              </h4>
                              {% endif %}
                        </div>
                  </div>
            </div>
            {% endfor %}
      </div>
      <div class="yp-container pt-[60px] md:pt-[78px]">
            <div class="swiper-pagination absolute !bottom-0 flex items-center gap-3 justify-center"></div>
      </div>
</div>
{% endif %}

{% endblock %}