<section id="{{block_anchor}}" class="{{color_profile}} ">
      <div class="section-wrapper {{ background_color }} {{ block_spacing }}">
            {% block block_content %}
            {# Block content goes here... #}
            {% endblock %}
      </div>
</section>